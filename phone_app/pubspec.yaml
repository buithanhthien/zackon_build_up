name: zackon_map
description: Zackon robot map viewer — live pose over 4G

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  web_socket_channel: ^2.4.0

flutter:
  uses-material-design: true

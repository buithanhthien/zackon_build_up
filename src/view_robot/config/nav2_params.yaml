amcl:
  ros__parameters:
    alpha1: 0.2
    alpha2: 0.2
    alpha3: 0.2
    alpha4: 0.2
    alpha5: 0.2
    set_initial_pose: true
    initial_pose_x: 0.0
    initial_pose_y: 0.0
    initial_pose_a: 0.0  # yaw in radians
    base_frame_id: "base_link"
    beam_skip_distance: 0.5
    beam_skip_error_threshold: 0.9
    beam_skip_threshold: 0.3
    do_beamskip: false
    global_frame_id: "map"
    lambda_short: 0.1
    laser_likelihood_max_dist: 2.0
    laser_max_range: 18.0
    laser_min_range: 0.05
    laser_model_type: "likelihood_field"
    max_beams: 60
    max_particles: 2000
    min_particles: 500
    odom_frame_id: "odom"
    pf_err: 0.05
    pf_z: 0.99
    recovery_alpha_fast: 0.0
    recovery_alpha_slow: 0.0
    resample_interval: 1
    robot_model_type: "nav2_amcl::DifferentialMotionModel"
    save_pose_rate: 1.0
    # save_pose_rate: 0.5
    sigma_hit: 0.2
    tf_broadcast: true
    transform_tolerance: 2.0
    introspection_mode: "disabled"
    # update_min_a: 0.2
    # update_min_d: 0.25
    update_min_d: 0.03
    update_min_a: 0.08  #4.6 degrees
    z_hit: 0.5
    z_max: 0.05
    z_rand: 0.5
    z_short: 0.05
    scan_topic: /scan_front_filter

bt_navigator:
  ros__parameters:
    global_frame: map
    robot_base_frame: base_link
    odom_topic: odom
    bt_loop_duration: 10
    filter_duration: 0.3
    default_server_timeout: 20
    wait_for_service_timeout: 1000
    introspection_mode: "disabled"
    navigators: ["navigate_to_pose", "navigate_through_poses"]
    navigate_to_pose:
      plugin: "nav2_bt_navigator::NavigateToPoseNavigator"
      enable_groot_monitoring: false
      groot_server_port: 1667
    navigate_through_poses:
      plugin: "nav2_bt_navigator::NavigateThroughPosesNavigator"
      enable_groot_monitoring: false
      groot_server_port: 1669
    bt_search_directories:
      - $(find-pkg-share nav2_bt_navigator)/behavior_trees
    # 'default_nav_through_poses_bt_xml' and 'default_nav_to_pose_bt_xml' are use defaults:
    # nav2_bt_navigator/navigate_to_pose_w_replanning_and_recovery.xml
    # nav2_bt_navigator/navigate_through_poses_w_replanning_and_recovery.xml
    # They can be set here or via a RewrittenYaml remap from a parent launch file to Nav2.

    # plugin_lib_names is used to add custom BT plugins to the executor (vector of strings).
    # Built-in plugins are added automatically
    # plugin_lib_names: []
    # plugin_lib_names:
    # - nav2_compute_path_to_pose_action_bt_node
    # - nav2_follow_path_action_bt_node
    # - nav2_back_up_action_bt_node
    # - nav2_spin_action_bt_node
    # - nav2_wait_action_bt_node
    # - nav2_clear_costmap_service_bt_node
    # - nav2_is_stuck_condition_bt_node
    # - nav2_goal_reached_condition_bt_node
    # - nav2_goal_updated_condition_bt_node
    # - nav2_initial_pose_received_condition_bt_node
    # - nav2_reinitialize_global_localization_service_bt_node
    # - nav2_rate_controller_bt_node
    # - nav2_distance_controller_bt_node
    # - nav2_speed_controller_bt_node
    # - nav2_truncate_path_action_bt_node
    # - nav2_goal_updater_node_bt_node
    # - nav2_recovery_node_bt_node
    # - nav2_pipeline_sequence_bt_node
    # - nav2_round_robin_node_bt_node
    # - nav2_transform_available_condition_bt_node
    # - nav2_time_expired_condition_bt_node
    # - nav2_distance_traveled_condition_bt_node

    error_code_name_prefixes:
      - assisted_teleop
      - backup
      - compute_path
      - dock_robot
      - drive_on_heading
      - follow_object
      - follow_path
      - nav_thru_poses
      - nav_to_pose
      - route
      - spin
      - smoother
      - undock_robot
      - wait


#####################REGULATED PURE PURSUIT CONTROLLER#####################
controller_server:
  ros__parameters:
    controller_frequency: 20.0
    min_x_velocity_threshold: 0.001
    min_y_velocity_threshold: 0.5
    min_theta_velocity_threshold: 0.001
    progress_checker_plugins: ["progress_checker"] # progress_checker_plugin: "progress_checker" For Humble and older
    goal_checker_plugins: ["goal_checker"]
    controller_plugins: ["FollowPath"]

    progress_checker:
      plugin: "nav2_controller::SimpleProgressChecker"
      required_movement_radius: 0.5
      movement_time_allowance: 20.0
    goal_checker:
      plugin: "nav2_controller::SimpleGoalChecker"
      xy_goal_tolerance: 0.25
      yaw_goal_tolerance: 0.25
      stateful: True
    FollowPath:
      plugin: "nav2_regulated_pure_pursuit_controller::RegulatedPurePursuitController"
      max_linear_vel: 0.5
      min_linear_vel: -0.2
      max_angular_vel: 1.5
      min_angular_vel: -1.5
      max_linear_accel: 2.5
      max_linear_decel: -2.5
      max_angular_accel: 12.0
      max_angular_decel: -12.0
      lookahead_dist: 0.6
      min_lookahead_dist: 0.3
      max_lookahead_dist: 0.9
      lookahead_time: 1.5
      rotate_to_heading_angular_vel: 1.5
      use_velocity_scaled_lookahead_dist: false
      min_approach_linear_velocity: 0.05
      approach_velocity_scaling_dist: 0.6
      use_collision_detection: true
      max_allowed_time_to_collision_up_to_carrot: 1.0
      use_regulated_linear_velocity_scaling: true
      use_fixed_curvature_lookahead: false
      curvature_lookahead_dist: 0.25
      use_cost_regulated_linear_velocity_scaling: false
      cost_scaling_dist: 0.3
      cost_scaling_gain: 1.0
      regulated_linear_scaling_min_radius: 0.9
      regulated_linear_scaling_min_speed: 0.25
      use_rotate_to_heading: true
      allow_reversing: false
      rotate_to_heading_min_angle: 0.785
      min_distance_to_obstacle: 0.0
      stateful: true
      use_dynamic_window: false


#####################ROTATION SHIM CONTROLLER WITH MPPI#####################
# controller_server:
#   ros__parameters:
#     controller_frequency: 20.0
#     odom_topic: /odometry/filtered
#     # These thresholds help prevent tiny cmd_vel from being treated as zero
#     # min_x_velocity_threshold: 0.01
#     # min_theta_velocity_threshold: 0.3

#     progress_checker_plugins: ["progress_checker"]
#     goal_checker_plugins: ["goal_checker"]
#     controller_plugins: ["FollowPath"]

#     progress_checker:
#       plugin: "nav2_controller::SimpleProgressChecker"
#       required_movement_radius: 0.5
#       movement_time_allowance: 20.0

#     goal_checker:
#       plugin: "nav2_controller::SimpleGoalChecker"
#       xy_goal_tolerance: 0.25
#       yaw_goal_tolerance: 0.25
#       stateful: true

#     # --- Wrapper controller: rotate-in-place when needed ---
#     FollowPath:
#       plugin: "nav2_rotation_shim_controller::RotationShimController"

#       # IMPORTANT: must be a STRING param
#       primary_controller: "nav2_mppi_controller::MPPIController"

#       # If robot heading differs from path heading more than this -> rotate in place first
#       angular_dist_threshold: 1.57 #0.785            # rad (~45 deg)

#       # How far ahead on the path to sample desired heading
#       forward_sampling_distance: 0.5           # meters

#       # Once within this heading error, hand off to MPPI
#       angular_disengage_threshold: 0.3925 #0.05 #0.3925      # rad (~22.5 deg)

#       # Rotate-in-place behavior
#       rotate_to_heading_angular_vel: 1.2       # rad/s (tune)
#       max_angular_accel: 12.0                   # rad/s^2
#       simulate_ahead_time: 1.0                 # seconds
#       use_path_orientations: false
#       closed_loop: true

#       # Optional: rotate at goal after reaching XY tolerance
#       rotate_to_goal_heading: true            # set true if you want final yaw alignment
#       rotate_to_heading_once: false


#       # --- Primary controller: MPPI ---
#       # primary_controller:
#       #   plugin: "nav2_mppi_controller::MPPIController"
#       # odom_topic: /odometry/filtered
#       # Time horizon
#       model_dt: 0.05
#       time_steps: 40              # start smaller for stability
#       batch_size: 2000            # start smaller then increase
#       iteration_count: 1

#       # DiffDrive limits
#       motion_model: "DiffDrive"
#       vx_max: 2.0 #0.5
#       vx_min: -0.20                 # start forward-only
#       vy_max: 0.0 #0.0                 # IMPORTANT for DiffDrive
#       wz_max: 3.0 #0.6                 # start conservative

#       # Sampling noise (DiffDrive)
#       vx_std: 0.2
#       vy_std: 0.0                # IMPORTANT
#       wz_std: 0.2

#       #acceleration limits
#       ax_max: 6.0
#       ax_min: -6.0
#       ay_max: 0.0
#       az_max: 6.0

#       # Cost / behavior
#       temperature: 0.35
#       gamma: 0.015
#       regenerate_noises: false
#       visualize: false   #true
#       publish_optimal_trajectory: true
#       publish_critics_stats: false

#       TrajectoryVisualizer:
#         trajectory_step: 5
#         time_step: 3
      
#       TrajectoryValidator:
#         plugin: "mppi::DefaultOptimalTrajectoryValidator"
#         collision_lookahead_time: 2.0
#         consider_footprint: true

#       critics:
#         [
#           "ConstraintCritic",
#           "CostCritic",
#           "GoalCritic",
#           "GoalAngleCritic",
#           "PathAlignCritic",
#           "PathFollowCritic",
#           "PathAngleCritic",
#           "PreferForwardCritic",
#           "ObstaclesCritic"
#         ]
  
#       ObstaclesCritic:
#         enabled: true
#         cost_power: 2
#         repulsion_weight:  4.0 #1.2
#         critical_weight: 20.0 #20.0
#         consider_footprint: true
#         collision_cost: 100000.0
#         collision_margin_distance: 0.7 #old is 0.4

#       ConstraintCritic:
#         enabled: true
#         cost_weight: 4.0
#         cost_power: 1

#       CostCritic:
#         enabled: true
#         cost_weight: 3.5
#         cost_power: 1
#         consider_footprint: true
#         collision_cost: 1000000.0
#         critical_cost: 300.0
#         near_goal_distance: 1.0
#         trajectory_point_step: 2

#       PathAlignCritic:
#         enabled: true
#         cost_weight: 10.0 #10
#         cost_power: 1
#         threshold_to_consider: 0.5
#         offset_from_furthest: 15
#         use_path_orientations: true       
#         trajectory_point_step: 4

#       PathFollowCritic:
#         enabled: true
#         cost_weight: 5.0 #5.0
#         cost_power: 1
#         threshold_to_consider: 1.0
#         offset_from_furthest: 5

#       PathAngleCritic:
#         enabled: true
#         cost_weight: 2.0
#         cost_power: 1
#         threshold_to_consider: 0.5
#         max_angle_to_furthest: 1.0
#         offset_from_furthest: 4
#         forward_preference: true
#         mode: 0 # 0: path angle, 1: goal angle

#       GoalCritic:
#         enabled: true
#         cost_weight: 5.0
#         cost_power: 1
#         threshold_to_consider: 1.0

#       GoalAngleCritic:
#         enabled: true
#         cost_weight: 3.0
#         cost_power: 1
#         threshold_to_consider: 0.5

#       PreferForwardCritic:
#         enabled: true
#         cost_weight: 5.0 #5.0
#         cost_power: 1
#         threshold_to_consider: 0.5

#####################################DWB CONTROLLER################################
# controller_server:
#   ros__parameters:
#     controller_frequency: 20.0
#     costmap_update_timeout: 0.30
#     min_x_velocity_threshold: 0.01
#     min_y_velocity_threshold: 0.001
#     min_theta_velocity_threshold: 0.001
#     failure_tolerance: 0.3
#     progress_checker_plugins: ["progress_checker"]
#     goal_checker_plugins: ["general_goal_checker"] # "precise_goal_checker"
#     controller_plugins: ["FollowPath"]
#     use_realtime_priority: false
#     speed_limit_topic: "speed_limit"

#     # Progress checker parameters
#     progress_checker:
#       plugin: "nav2_controller::SimpleProgressChecker"
#       required_movement_radius: 0.5
#       movement_time_allowance: 10.0
#     # Goal checker parameters
#     #precise_goal_checker:
#     #  plugin: "nav2_controller::SimpleGoalChecker"
#     #  xy_goal_tolerance: 0.25
#     #  yaw_goal_tolerance: 0.25
#     #  stateful: True

#     general_goal_checker:
#       stateful: True
#       plugin: "nav2_controller::SimpleGoalChecker"
#       xy_goal_tolerance: 0.25
#       yaw_goal_tolerance: 0.25

#     FollowPath:
#       plugin: "dwb_core::DWBLocalPlanner"
#       debug_trajectory_details: True
#       min_vel_x: 0.0
#       min_vel_y: 0.0
#       max_vel_x: 0.7 # old 0.5
#       max_vel_y: 0.0
#       max_vel_theta: 1.2 # old 1.0

#       min_speed_xy: 0.0
#       max_speed_xy: 0.3 # old 0.26
#       min_speed_theta: 0.0

#       acc_lim_x: 1.5 # old 2.5
#       acc_lim_y: 0.0
#       acc_lim_theta: 2.5 # old 3.2
#       decel_lim_x: -2.5
#       decel_lim_y: 0.0
#       decel_lim_theta: -2.5 # old -3.2

#       vx_samples: 24 # old 20
#       vy_samples: 1 # old 5
#       vtheta_samples: 40 # old 20

#       sim_time: 2.2 # old 1.7
#       linear_granularity: 0.05
#       angular_granularity: 0.025

#       transform_tolerance: 0.2
#       xy_goal_tolerance: 0.25

#       trans_stopped_velocity: 0.05 # old 0.25
#       short_circuit_trajectory_evaluation: True
#       stateful: True

#       critics: 
#         [
#           "RotateToGoal", 
#           "Oscillation", 
#           "BaseObstacle",
#           "ObstacleFootprint", 
#           "GoalAlign", 
#           "PathAlign", 
#           "PathDist", 
#           "GoalDist"
#         ]

#       # --- Ne vat can ---
#       BaseObstacle.scale: 0.10 # 0.02
#       ObstacleFootprint.scale: 0.12 # 0.08

#       # --- Giu bam path ---
#       PathAlign.scale: 24.0 # old 32.0
#       PathAlign.forward_point_distance: 0.3 # old 0.1
#       GoalAlign.scale: 18.0 # old 24.0
#       GoalAlign.forward_point_distance: 0.3 # old 0.1

#       PathDist.scale: 20.0 # old 32.0
#       GoalDist.scale: 18.0 # old 24.0

#       RotateToGoal.scale: 24.0 # old 32.0
#       RotateToGoal.slowing_factor: 5.0
#       RotateToGoal.lookahead_time: -1.0

local_costmap:
  local_costmap:
    ros__parameters:
      update_frequency: 20.0
      publish_frequency: 10.0
      global_frame: odom
      robot_base_frame: base_link
      rolling_window: true
      width: 10
      height: 10
      resolution: 0.05
      footprint: "[ [0.30, 0.25], [0.30, -0.25], [-0.30, -0.25], [-0.30, 0.25] ]"
      plugins: ["obstacle_layer","inflation_layer"]
      # filters: ["keepout_filter"]
      # keepout_filter:
      #   plugin: "nav2_costmap_2d::KeepoutFilter"
      #   enabled: False #KEEPOUT_ZONE_ENABLED  
      #   filter_info_topic: "keepout_costmap_filter_info"
      #   override_lethal_cost: True
      #   lethal_override_cost: 200
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.7 #0.5 #0.7
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        footprint_clearing_enabled: True
        observation_sources: scan
        scan:
          # A relative topic will be appended to the parent of the global_costmap namespace.
          # For example:
          #   * User chosen namespace is `tb4`.
          #   * User chosen topic is `scan`.
          #   * Topic will be remapped to `/tb4/scan_front_filter` without `global_costmap`.
          #   * Use global topic `/scan` if you do not wish the node namespace to apply
          topic: /scan_front_filter
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          # sensor_frame: lidar_link
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      # voxel_layer:
      #   plugin: "nav2_costmap_2d::VoxelLayer"
      #   enabled: True
      #   publish_voxel_map: True
      #   footprint_clearing_enabled: True
      #   origin_z: 0.0
      #   z_resolution: 0.05
      #   z_voxels: 16
      #   max_obstacle_height: 2.0
      #   mark_threshold: 0
      #   observation_sources: scan
      #   scan:
      #     # A relative topic will be appended to the parent of the local_costmap namespace.
      #     # For example:
      #     #   * User chosen namespace is `tb4`.
      #     #   * User chosen topic is `scan`.
      #     #   * Topic will be remapped to `/tb4/scan` without `local_costmap`.
      #     #   * Use global topic `/scan` if you do not wish the node namespace to apply
      #     topic: /scan
      #     max_obstacle_height: 2.0
      #     clearing: True
      #     marking: True
      #     data_type: "LaserScan"
      #     sensor_frame: lidar_link   # đúng tên frame lidar của bạn
      #     raytrace_max_range: 3.0
      #     raytrace_min_range: 0.2
      #     obstacle_max_range: 2.5
      #     obstacle_min_range: 0.2
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      always_send_full_costmap: True
      introspection_mode: "disabled"


global_costmap:
  global_costmap:
    ros__parameters:
      update_frequency: 5.0
      publish_frequency: 2.0
      global_frame: map
      robot_base_frame: base_link
      rolling_window: false
      # width: 200
      # height: 200
      footprint: "[ [0.30, 0.25], [0.30, -0.25], [-0.30, -0.25], [-0.30, 0.25] ]"
      # resolution: 0.05
      track_unknown_space: true
      plugins: ["static_layer", "obstacle_layer", "inflation_layer"]
      #filters: ["keepout_filter", "speed_filter"]
      # keepout_filter:
      #   plugin: "nav2_costmap_2d::KeepoutFilter"
      #   enabled:  False #KEEPOUT_ZONE_ENABLED  # moi sua #True
      #   filter_info_topic: "keepout_costmap_filter_info"
      #   override_lethal_cost: True
      #   lethal_override_cost: 200
      speed_filter:
        plugin: "nav2_costmap_2d::SpeedFilter"
        enabled: False #SPEED_ZONE_ENABLED  #moi sua #True
        filter_info_topic: "speed_costmap_filter_info"
        speed_limit_topic: "speed_limit"
      obstacle_layer:
        plugin: "nav2_costmap_2d::ObstacleLayer"
        enabled: True
        footprint_clearing_enabled: True
        observation_sources: scan
        scan:
          # A relative topic will be appended to the parent of the global_costmap namespace.
          # For example:
          #   * User chosen namespace is `tb4`.
          #   * User chosen topic is `scan`.
          #   * Topic will be remapped to `/tb4/scan` without `global_costmap`.
          #   * Use global topic `/scan` if you do not wish the node namespace to apply
          topic: /scan_front_filter
          max_obstacle_height: 2.0
          clearing: True
          marking: True
          data_type: "LaserScan"
          # sensor_frame: lidar_link
          raytrace_max_range: 3.0
          raytrace_min_range: 0.0
          obstacle_max_range: 2.5
          obstacle_min_range: 0.0
      static_layer:
        plugin: "nav2_costmap_2d::StaticLayer"
        map_subscribe_transient_local: True
      inflation_layer:
        plugin: "nav2_costmap_2d::InflationLayer"
        cost_scaling_factor: 3.0
        inflation_radius: 0.7 #0.5 #0.6
      always_send_full_costmap: True
      introspection_mode: "disabled"

# The yaml_filename does not need to be specified since it going to be set by defaults in launch.
# If you'd rather set it in the yaml, remove the default "map" value in the launch file(s).
# file & provide full path to map below. If CLI map configuration or launch default is provided, that will be used.
map_server:
  ros__parameters:
    yaml_filename: "/home/khoaiuh/zackon_ws/src/MangoMobileRobot/src/view_robot_pkg/maps/khoi_sofa_map4.yaml"
    use_sim_time: False
    introspection_mode: "disabled"

keepout_filter_mask_server:
  ros__parameters:
    topic_name: "keepout_filter_mask"
    # yaml_filename: ""

keepout_costmap_filter_info_server:
  ros__parameters:
    type: 0
    filter_info_topic: "keepout_costmap_filter_info"
    mask_topic: "keepout_filter_mask"
    base: 0.0
    multiplier: 1.0

speed_filter_mask_server:
  ros__parameters:
    topic_name: "speed_filter_mask"
    # yaml_filename: ""

speed_costmap_filter_info_server:
  ros__parameters:
    type: 1
    filter_info_topic: "speed_costmap_filter_info"
    mask_topic: "speed_filter_mask"
    base: 100.0
    multiplier: -1.0

map_saver:
  ros__parameters:
    save_map_timeout: 5.0
    free_thresh_default: 0.25
    occupied_thresh_default: 0.65
    map_subscribe_transient_local: True
    introspection_mode: "disabled"

#######################SMAC PLANNER LATTICE#####################
# planner_server:
#   ros__parameters:
#     planner_plugins: ["GridBased"]

#     GridBased:
#       plugin: "nav2_smac_planner::SmacPlannerLattice" # In Iron and older versions, "/" was used instead of "::"
#       allow_unknown: true                 # Allow traveling in unknown space
#       tolerance: 0.25                     # dist-to-goal heuristic cost (distance) for valid tolerance endpoints if exact goal cannot be found.
#       max_iterations: 1000000             # Maximum total iterations to search for before failing (in case unreachable), set to -1 to disable
#       max_on_approach_iterations: 1000    # Maximum number of iterations after within tolerances to continue to try to find exact solution
#       max_planning_time: 5.0              # Max time in s for planner to plan, smooth
#       analytic_expansion_ratio: 3.5       # The ratio to attempt analytic expansions during search for final approach.
#       analytic_expansion_max_length: 3.0  # For Hybrid/Lattice nodes The maximum length of the analytic expansion to be considered valid to prevent unsafe shortcutting
#       analytic_expansion_max_cost: 200.0  # The maximum single cost for any part of an analytic expansion to contain and be valid, except when necessary on approach to goal
#       analytic_expansion_max_cost_override: false  #  Whether or not to override the maximum cost setting if within critical distance to goal (ie probably required)
#       downsample_obstacle_heuristic: true   # Downsample the obstacle map dynamic programming distance expansion heuristic to speed up search at the cost of some path quality
#       use_quadratic_cost_penalty: false   # Use quadratic cost penalty for traversal and heuristic cost computations rather than linear
#       reverse_penalty: 2.0                # Penalty to apply if motion is reversing, must be => 1
#       change_penalty: 0.05                # Penalty to apply if motion is changing directions (L to R), must be >= 0
#       non_straight_penalty: 1.05          # Penalty to apply if motion is non-straight, must be => 1
#       cost_penalty: 2.0                   # Penalty to apply to higher cost areas when adding into the obstacle map dynamic programming distance expansion heuristic. This drives the robot more towards the center of passages. A value between 1.3 - 3.5 is reasonable.
#       rotation_penalty: 5.0               # Penalty to apply to in-place rotations, if minimum control set contains them
#       retrospective_penalty: 0.015
#       lattice_filepath: "/opt/ros/jazzy/share/nav2_smac_planner/sample_primitives/5cm_resolution/0.5m_turning_radius/diff/output.json"                # The filepath to the state lattice graph
#       lookup_table_size: 20.0             # Size of the dubin/reeds-sheep distance window to cache, in meters.
#       cache_obstacle_heuristic: false     # Cache the obstacle map dynamic programming distance expansion heuristic between subsequent replannings of the same goal location. Dramatically speeds up replanning performance (40x) if costmap is largely static.
#       allow_reverse_expansion: false      # If true, allows the robot to use the primitives to expand in the mirrored opposite direction of the current robot's orientation (to reverse).
#       coarse_search_resolution: 1         # Number of bins to skip when doing a coarse search for the path. Only used for all_direction goal heading mode.
#       goal_heading_mode: "BIDIRECTIONAL"        # DEFAULT, BIDIRECTIONAL, ALL_DIRECTION
#       smooth_path: True                   # If true, does a simple and quick smoothing post-processing to the path
#       smoother:
#         max_iterations: 500
#         w_smooth: 0.3
#         w_data: 0.2
#         tolerance: 1.0e-5
#         do_refinement: true
#         refinement_num: 2

######################SMAC PLANNER 2D################################
planner_server:
  ros__parameters:
    planner_plugins: ["GridBased"]

    GridBased:
      plugin: "nav2_smac_planner::SmacPlanner2D" # In Iron and older versions, "/" was used instead of "::"
      tolerance: 0.125                      # tolerance for planning if unable to reach exact pose, in meters
      downsample_costmap: false             # whether or not to downsample the map
      downsampling_factor: 1                # multiplier for the resolution of the costmap layer (e.g. 2 on a 5cm costmap would be 10cm)
      allow_unknown: true                   # allow traveling in unknown space
      max_iterations: 1000000               # maximum total iterations to search for before failing (in case unreachable), set to -1 to disable
      max_on_approach_iterations: 1000      # maximum number of iterations to attempt to reach goal once in tolerance
      max_planning_time: 2.0                # max time in s for planner to plan, smooth
      cost_travel_multiplier: 2.0           # Cost multiplier to apply to search to steer away from high cost areas. Larger values will place in the center of aisles more exactly (if non-`FREE` cost potential field exists) but take slightly longer to compute. To optimize for speed, a value of 1.0 is reasonable. A reasonable tradeoff value is 2.0. A value of 0.0 effective disables steering away from obstacles and acts like a naive binary search A*.
      use_final_approach_orientation: false # Whether to set the final path pose at the goal's orientation to the requested orientation (false) or in line with the approach angle so the robot doesn't rotate to heading (true)
      smoother:
        max_iterations: 1000
        w_smooth: 0.3
        w_data: 0.2
        tolerance: 1.0e-10

#################THETA* PLANNER#####################
# planner_server:
#   ros__parameters:
#     planner_plugin_types: ["nav2_theta_star_planner/ThetaStarPlanner"]
#     use_sim_time: True
#     planner_plugin_ids: ["GridBased"]
#     GridBased:
#       how_many_corners: 8
#       w_euc_cost: 1.0
#       w_traversal_cost: 2.0


# planner_server:
#   ros__parameters:
#     allow_partial_planning: false
#     expected_planner_frequency: 20.0
#     planner_plugins: ["GridBased"]
#     costmap_update_timeout: 1.0
#     introspection_mode: "disabled"
#     GridBased:
#       plugin: "nav2_navfn_planner::NavfnPlanner"
#       tolerance: 0.5
#       use_astar: false
#       allow_unknown: true

smoother_server:
  ros__parameters:
    smoother_plugins: ["simple_smoother", "route_smoother"]
    simple_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      refinement_num: 2
      # True for Hybrid A* or State Lattice to not smooth over directional changes
      enforce_path_inversion: True
      do_refinement: True
    route_smoother:
      plugin: "nav2_smoother::SimpleSmoother"
      tolerance: 1.0e-10
      max_its: 1000
      refinement_num: 5
      # False for Route Server or others where smoothing out sharp corners is OK
      enforce_path_inversion: False
      do_refinement: True

behavior_server:
  ros__parameters:
    local_costmap_topic: local_costmap/costmap_raw
    global_costmap_topic: global_costmap/costmap_raw
    local_footprint_topic: local_costmap/published_footprint
    global_footprint_topic: global_costmap/published_footprint
    cycle_frequency: 10.0
    behavior_plugins:
      ["spin", "backup", "drive_on_heading", "assisted_teleop", "wait"]
    spin:
      plugin: "nav2_behaviors::Spin"
    backup:
      plugin: "nav2_behaviors::BackUp"
      acceleration_limit: 2.5
      deceleration_limit: -2.5
      minimum_speed: 0.10
    drive_on_heading:
      plugin: "nav2_behaviors::DriveOnHeading"
      acceleration_limit: 2.5
      deceleration_limit: -2.5
      minimum_speed: 0.10
    wait:
      plugin: "nav2_behaviors::Wait"
    assisted_teleop:
      plugin: "nav2_behaviors::AssistedTeleop"
    local_frame: odom
    global_frame: map
    robot_base_frame: base_link
    transform_tolerance: 0.1
    simulate_ahead_time: 2.0
    max_rotational_vel: 1.0
    min_rotational_vel: 0.4
    rotational_acc_lim: 3.2

waypoint_follower:
  ros__parameters:
    loop_rate: 20
    stop_on_failure: false
    introspection_mode: "disabled"
    waypoint_task_executor_plugin: "wait_at_waypoint"
    wait_at_waypoint:
      plugin: "nav2_waypoint_follower::WaitAtWaypoint"
      enabled: True
      waypoint_pause_duration: 200

route_server:
  ros__parameters:
    # The graph_filepath does not need to be specified since it going to be set by defaults in launch.
    # If you'd rather set it in the yaml, remove the default "graph" value in the launch file(s).
    # file & provide full path to map below. If graph config or launch default is provided, it is used
    # graph_filepath: $(find-pkg-share nav2_route)/graphs/aws_graph.geojson
    boundary_radius_to_achieve_node: 1.0
    radius_to_achieve_node: 2.0
    smooth_corners: true
    operations: ["AdjustSpeedLimit", "ReroutingService", "CollisionMonitor"]
    ReroutingService:
      plugin: "nav2_route::ReroutingService"
    AdjustSpeedLimit:
      plugin: "nav2_route::AdjustSpeedLimit"
    CollisionMonitor:
      plugin: "nav2_route::CollisionMonitor"
      max_collision_dist: 3.0
    edge_cost_functions: ["DistanceScorer", "CostmapScorer"]
    DistanceScorer:
      plugin: "nav2_route::DistanceScorer"
    CostmapScorer:
      plugin: "nav2_route::CostmapScorer"

velocity_smoother:
  ros__parameters:
    smoothing_frequency: 20.0
    scale_velocities: False
    feedback: "OPEN_LOOP"
    max_velocity: [0.5, 0.0, 2.0]
    min_velocity: [-0.5, 0.0, -2.0]
    max_accel: [2.5, 0.0, 3.2]
    max_decel: [-2.5, 0.0, -3.2]
    odom_topic: "odom"
    odom_duration: 0.1
    deadband_velocity: [0.0, 0.0, 0.0]
    velocity_timeout: 1.0

collision_monitor:
  ros__parameters:
    enabled: True
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    cmd_vel_in_topic: "cmd_vel_smoothed"
    cmd_vel_out_topic: "cmd_vel"
    state_topic: "collision_monitor_state"
    transform_tolerance: 0.2
    source_timeout: 1.0
    base_shift_correction: True
    stop_pub_timeout: 2.0
    # Polygons represent zone around the robot for "stop", "slowdown" and "limit" action types,
    # and robot footprint for "approach" action type.
    polygons: ["FootprintApproach"]
    FootprintApproach:
      type: "polygon"
      action_type: "approach"
      footprint_topic: "local_costmap/published_footprint"
      time_before_collision: 1.2
      simulation_time_step: 0.1
      min_points: 6
      visualize: False
      enabled: True
    observation_sources: ["scan"]
    scan:
      type: "scan"
      topic: /scan_front_filter
      min_height: 0.15
      max_height: 2.0
      enabled: True

# docking_server:
#   ros__parameters:
#     controller_frequency: 50.0
#     initial_perception_timeout: 5.0
#     wait_charge_timeout: 5.0
#     dock_approach_timeout: 30.0
#     undock_linear_tolerance: 0.05
#     undock_angular_tolerance: 0.1
#     max_retries: 3
#     base_frame: "base_link"
#     fixed_frame: "odom"
#     dock_prestaging_tolerance: 0.5
#     introspection_mode: "disabled"

    # # Types of docks
    # dock_plugins: ["simple_charging_dock"]
    # simple_charging_dock:
    #   plugin: "opennav_docking::SimpleChargingDock"
    #   docking_threshold: 0.05
    #   staging_x_offset: -0.7
    #   use_external_detection_pose: true
    #   use_battery_status: false # true
    #   use_stall_detection: false # true
    #   dock_direction: "forward"

    #   external_detection_timeout: 1.0
    #   external_detection_translation_x: -0.18
    #   external_detection_translation_y: 0.0
    #   external_detection_rotation_roll: -1.57
    #   external_detection_rotation_pitch: -1.57
    #   external_detection_rotation_yaw: 0.0
    #   filter_coef: 0.1

    # Dock instances
    # The following example illustrates configuring dock instances.
    # docks: ['home_dock']  # Input your docks here
    # home_dock:
    #   type: 'simple_charging_dock'
    #   frame: map
    #   pose: [0.0, 0.0, 0.0]

    # controller:
    #   k_phi: 3.0
    #   k_delta: 2.0
    #   v_linear_min: 0.15
    #   v_linear_max: 0.15
    #   use_collision_detection: true
    #   costmap_topic: "local_costmap/costmap_raw"
    #   footprint_topic: "local_costmap/published_footprint"
    #   transform_tolerance: 0.1
    #   projection_time: 5.0
    #   simulation_step: 0.1
    #   dock_collision_threshold: 0.3

loopback_simulator:
  ros__parameters:
    base_frame_id: "base_link"
    odom_frame_id: "odom"
    map_frame_id: "map"
    scan_frame_id: "base_scan" # tb4_loopback_simulator.launch.py remaps to 'rplidar_link'
    update_duration: 0.02
    scan_range_min: 0.05
    scan_range_max: 30.0
    scan_angle_min: -3.1415
    scan_angle_max: 3.1415
    scan_angle_increment: 0.02617
    scan_use_inf: true


    read all above and compare to /home/khoaiuh/thien_ws/src/view_robot/config/nav2_params.yaml then replace